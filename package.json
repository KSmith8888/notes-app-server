{
    "name": "notes-app-server",
    "version": "1.0.0",
    "description": "A notes app made with NodeJS, Express and MongoDB",
    "type": "module",
    "engines": {
        "node": ">=20.10.0"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node --env-file=.env index.js",
        "dev": "node --env-file=.env --watch index.js"
    },
    "author": "Kevyn Smith",
    "license": "MIT",
    "dependencies": {
        "express": "^4.18.2",
        "mongoose": "^8.1.0"
    }
}
